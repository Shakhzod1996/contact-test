export interface IApplication {
    _id: string;
    isDeleted: boolean;
    description: string;
    name: string;
    phoneNumber: string;
    email: string;
    createdAt: string;
    updatedAt: string;
}
